services:
  - type: web
    name: fastpitch-scraper
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn server:app"
    plan: free
    autoDeploy: true
    branch: main